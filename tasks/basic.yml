- name: basic - Install basic components
  apt:
    name: "{{ packages }}"
  vars:
    packages:
    - neovim
    - i3
    - sway
    - swaylock
    - tmux
    - htop
    - xclip
    - links
    - iftop
    - rsync
    - maim
    - xterm
    - imagemagick
  become: true
  become_user: root
